- hosts:
    - kube-master
    - kube-worker
    - new-worker

  tasks:
    - block:
        - name: write file
          shell: echo "the ansibleParameter2={{ ansibleParameter2 }}" >> {{ ansibleParameter3 }}
      when: addon_plugin_action == "install"

    - block:
        - name: write file
          shell: rm -rf {{ ansibleParameter3 }}
      when: addon_plugin_action == "uninstall"